{
  "name": "appband-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end tests for the AppBand dashboard. Dev-only tooling — NOT shipped and NOT a backend dependency; the Python backend stays stdlib-only. Run with: npm install && npx playwright install chromium && npm test",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}

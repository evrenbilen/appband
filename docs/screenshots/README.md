# Screenshots

Drop the three screenshots referenced by the project README here:

| File | What it shows |
|---|---|
| `dashboard.png`  | Full dashboard page (LIVE panel + Time Series + By Domain + By App) |
| `popover.png`    | Menu bar popover (LIVE panel with Download / Upload + network chip + menu) |
| `menubar.png`    | The compact menu bar item itself (icon + ↓/↑ Mbps) |

Recommended capture sizes (after retina scaling):

- `dashboard.png`: ~1600 × 1200 (full browser window, content area only)
- `popover.png`:   ~600 × 700
- `menubar.png`:   ~600 × 80 (a tight horizontal strip showing the icon + numbers)
